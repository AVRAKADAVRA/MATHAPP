import 'package:flutter/material.dart';

class LevelPage6 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Level 6'),
          centerTitle: true
      ),
      backgroundColor: Colors.blue,
      body: Center(
        child: Text('This is Level 6'),
      ),
    );
  }
}