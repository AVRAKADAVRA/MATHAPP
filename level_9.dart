import 'package:flutter/material.dart';

class LevelPage9 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Level 9'),
          centerTitle: true
      ),
      backgroundColor: Colors.blue,
      body: Center(
        child: Text('This is Level 9'),
      ),
    );
  }
}